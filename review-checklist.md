# 独立审核清单（Step 5.5）

生成后的自检（Step 5）不能代替本清单。审核目标是**找不一致**，禁止把自检勾选复制成结论。

审核结论只写到生成物同级 `review/{主文件名}-审核-YYYYMMDD.md`，**禁止**写入设计说明正文。

有任意 P0 未清（修不掉）：不准声称已完成。

**当场修的范围：** 结构 / 字号档 / 图表类型 / 锚点 / 换皮 / JS 报错（P0、P1）。  
**不要打磨的范围：** 材质类 P2（光晕、灯管密度、ECharts 地图材质）标「未覆盖」后停止改皮肤。  
用户说「不要再打磨 / 停 / 换模型」：禁止再改 CSS 与图表皮肤。

---

## 公共 P0（HTML / Vue 都算）

出现任一条即为 P0：

- 确认单里的区块缺失、指标名/锚点数字被改写
- 有参考图时：未跑 skill 目录的 `scripts/inspect-source.js`（或等价 `file`+像素），或把宽 &lt; 1600 / JPEG 冒充 PNG 的聊天压缩图拉伸后写成确定 gutter
- 有参考图时：壳图与内容截图混用坐标系（用壳图估列宽、或把整页截图的「到屏幕左缘」直接写成 `.stage` padding）
- 有参考图时：卡片数量、行列、**卡内结构**、关键字段、**标题/装饰相对位置**、**字号层级**与分区放大后的图不一致（配色除外，配色跟 token；字号不跟 token 默认区间改小）
- 有参考图且图中有标题匾/标题条时：原型用自造两侧扫光或纯字顶替该容器，或把匾下的组织数字/页签拆成独立通栏导致左右列顶标下移
- 有参考图时：顶栏未做放大 clip、clip 不是目标区块、或写不出标题容器形态仍继续生成
- 有参考图且图中面板有标题条 / 图标 / 筛选附加控件时：原型用通用四角标或空心圈顶替标题条，或把图标、标签、轮播收成只有数字
- 有参考图时：未写出卡内标题条形态、数字/标签上下关系、图标与附加控件仍继续生成
- 有参考图或现网时：标题或主数字明显小/大一档，或与现网已写死字号对不上
- 有参考图时：未做「1920 原型 ↔ 参考图」同区块并排裁切，或并排后仍有结构性排布偏差或字号差一档
- 为「更好读」改掉参考图排布（如把右侧纵排同比环比改成数字下方通栏）
- 恒等式不成立（分项之和 ≠ 汇总；文档/图已自洽的数被另编）
- 控制台有未捕获 JS 报错（含切换筛选、打开弹窗、改窗口大小）
- 深色主题出现白底控件（tooltip / 弹窗 / **下拉浮层** / date-picker）
- 弹窗、遮罩、toast 不在缩放画布内，或 Esc/遮罩无法关掉导致错位
- 审核报告复述 Step 5 勾选、没有对照确认单/参考图的具体差异
- 有参考图时：审核没有「区块视觉差清单」，清单为空，或**每一块都只写「该块已同构」**却写通过
- 有参考图时：只交恒等式 / `js-errors none` / 无横滚，未并排看图
- 有参考图时：主数字色相与图不一致（如利润绿灯管做成青色）。色相是结构，具体 hex 仍用 token
- 有参考图时：用 `templates/screen.html` 默认玻璃卡、独立深色筛选通栏，或套 `24% 1fr 24%`，顶替图上的嵌套筛选或弱框 KPI（换皮）
- 有参考图且仓库已有壳图时：又用 CSS 侧轨/顶栏电路去叠一套与壳重复的装饰
- 有参考图时：底图是柱+线或堆叠柱，原型用通用折线顶替
- 参考图整图宽 &lt; 1600px，确认单未声明「精度不足 / 量寸不可靠」仍按已看清写通过
- 有参考图时：把 ECharts 默认地图材质、灯管发光弱一档等能力边界项写成「已同构 / 无差」来换取通过

---

## HTML 节

### P0

- 弹窗/遮罩/toast 不在 `#screen`（或等价画布）内
- `transform: scale` 后弹窗不随画布居中
- 地图/图表 CDN 失败后，再切筛选仍对已销毁实例调 `setOption`/`resize`（会抛错）
- 双击打开的路径依赖未写清，且失败时整页白屏（数字区也应还能看）

### P1

- 原生 `<select>` 展开后系统白底（应用主题色自定义列表，或至少在审核里标明）
- 翻牌/KPI 切时间后位数跳动、无补位
- 有参考图但图表类型明显不同（如原图整圆环做成半圆 gauge）
- 假交互只 toast、画面/数据完全不变（确认单写了要变的项）
- **主题模式** CountUp / 入场动效缺失；复刻时 KPI 形态与图不符（翻牌做成 CountUp 也算）

### P2

写进审核即可。结构/字号差一档不算本条（算 P0）。

- 与读数/结构无关的细微光晕、线宽、1–2px 字距 → **标未覆盖后停止打磨**，禁止循环改皮肤
- 南海/插图用文字框代替地图（有 geo 插图更好，不挡主图即可）
- 视频占位未做成参考图那种播放器壳（无片源时允许占位，但外框位置仍须同构）

### 建议取证

- `node ~/.cursor/skills/bi-dashboard-generator/scripts/inspect-source.js` 源图输出（有参考图必贴）
- 无头浏览器：基准分辨率 + 至少 2 个其它分辨率截图
- 有参考图：同区块裁切并排（ref 来自原图、proto 来自 HTML `[data-review]`）；视觉差清单每块一条可核对差或「未覆盖：…」
- 手算或脚本核 3～5 条恒等式

---

## Vue 节

### P0

- 只有 `screen.vue` 注释、缺 `package.json` / `vite.config` / `variables.less` / `colors.js` / `useScreenAdapt` / 面板与图表组件，**不能 `npm i && npm run dev` 跑起来**
- 有 HTML 原型时：区块、栅格比例、指标、交互与原型不是 1:1（含把复刻 HTML 拉回玻璃 PanelBox）
- Element Plus 深色主题下，下拉面板 / 输入框 / Dialog / DatePicker 浮层仍是默认白底
- LESS 变量与 `colors.js` 色值对不上（同源镜像被破坏）
- 跨页/跨组件时间或筛选靠 props 层层传，未走 Pinia（确认单要求全局联动时）

### P1

- 屏幕适配不是整页 `transform: scale`（或用户要求的等比留边）
- API 占位无 mock 开关、无字段映射注释
- `resize` 未在窗口变化时调用，或组件卸载未 `dispose` 图表
- 深色覆盖只写了页面、没收浮层 teleport 到 body 的节点

### P2

能力边界允许的「未覆盖」标明即可、不必改；其余与结构有关的当场修。

- 路由/权限未做（能力边界允许，审核里标明「未覆盖」即可，不算 P0）
- 按需自动引入未配，但全量注册了 Element Plus 且深色仍正确

### 建议取证

- 贴 `npm run dev` 能打开的 URL，或贴启动失败日志
- 打开一处 `el-select` / `el-date-picker` 截图，证明浮层不是白底
- 与 HTML 原型同分辨率截图并排，列差异

---

## 审核文件骨架（照抄后填）

```markdown
# {标题} 审核

- 日期：YYYY-MM-DD
- 对象：{html 路径或 Vue 工程入口}
- 对照：确认单 / 参考图（有无）/ 硬性规则 / 本清单
- 源图鉴定：{inspect-source 输出摘要}

## 结论

不通过（修不掉：…） | 通过（P0/P1 已修完复审，或仅剩已标明的能力边界未覆盖）

## P0

- …

## P1 / P2

- …

## 区块视觉差清单（有参考图必填）

每块写 1 条可核对差，或「未覆盖：…」。禁止整表只写「该块已同构」。

| 区块 | ref | proto | 可见差 |
|---|---|---|---|
| 顶栏 | review/ref-topbar.png | review/proto-topbar.png | … |

## 证据

- 恒等式：…
- 截图：…
- 命令输出：…

## 复审（改完后追加）

- 日期：
- 仍未清：
```
