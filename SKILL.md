---
name: bi-dashboard-generator
description: 生成风格统一的数据可视化大屏（深色 BI 科技风为主，可选多主题）。支持从一句话需求、布局描述、参考图或需求文档生成高保真 HTML 原型（含 mock 数据与假交互），可选转 Vue3 + Element Plus + ECharts + LESS + Pinia（Vite 构建）工程代码。当用户要求创建数据大屏、可视化大屏、驾驶舱、BI 看板、监控大屏、dashboard 时使用。
---

# 可视化大屏生成器

生成风格统一、专业观感的数据可视化大屏。默认产出**单文件 HTML 高保真原型**（浏览器直接打开预览），确认效果后可转 Vue3 工程代码。

## 能力边界（生成前告知用户）

| 层次 | 是否覆盖 |
|---|---|
| 静态展示（布局/图表/KPI/配色） | ✅ 完整生成 |
| 页面交互（弹窗/穿透跳转/筛选） | ✅ HTML 内 mock 数据假交互 |
| 真实数据对接 | ⚠️ 仅生成 API 占位与字段映射表，需开发接入 |
| 工程化（路由/状态/权限） | ⚠️ Vue 模式给骨架，细节需开发 |

## 工作流程

```
Step 1 识别入口 → Step 2 输出方案确认单 → Step 3 确认主题与输出形式
→ Step 3.5 骨架预览（复杂/多页项目） → Step 4 生成 → Step 5 自检
→ Step 5.5 独立审核（HTML / Vue 均要） → Step 6 交付与迭代
```

### Step 1：识别需求入口（四选一，自动判断）

**入口① 一句话需求**（小白用户）：
用户只说了业务（如"做一个奶茶店销售大屏"）。此时**由 AI 代做专业决策**：根据业务推断指标 → 选布局骨架 → 配组件，然后用人话向用户描述方案。禁止反问用户"要什么布局/什么图表"这类专业问题。

业务判断本身也可能出错（尤其跨行业/混合业务描述），**方案确认单必须显性列出「业务识别 + 默认指标包」**（如"识别为医药经营分析类，默认指标：营收/利润/存货/动销品种/批文"），让用户先确认业务判断本身，而非只确认布局，避免指标包整体选错却在生成完才发现。

**入口② 布局描述**：用户明确写了分区与组件（"左边放 KPI，中间放趋势图…"）。直接解析映射到栅格骨架，缺失处才追问。

**入口③ 参考图/原型截图**：进入**复刻模式**（不是主题模式）。分析布局分区与组件类型并复刻；色值仍取自主题 token，**不照抄原图 hex**。不照抄 hex ≠ 换皮：几何、字号档、装饰形态、筛选控件形态、主数字色相、列内疏密必须跟图走。无参考图才走**主题模式**（token + `templates/screen.html` 的默认脸）。

- **必须先分区放大再复刻，禁止只凭整图印象转译**：参考图通常只有 1000–2000px 宽，面板级文字（企业名、指标名、状态脚注）在该尺度下不可读，整图直接转译必然漏读或猜错区块数量。做法：把图按区块（顶栏 / 每个面板）切分并放大 4–12 倍逐块读取。**裁切只用 Playwright（或等价）按坐标 `clip`**，裁完必须先目视确认是目标区块再读；`sips --cropOffset` 等在部分系统上会裁错，裁错或裁切失败时**禁止继续生成**，重裁或停下来问用户。逐块确认**卡片数量、排布行列、卡内结构、字段构成、状态文案、装饰相对位置、字号层级（类别标题 / 主数字 / 脚注）**后再动手。
- **顶栏死门（有参考图时）**：动手前必须单独 clip 出顶栏放大图，并写明标题容器形态：梯形匾 / 现网标题条 / 纯字。写不出形态 = 读不清 = **停下来问用户，禁止开写**。确认单禁止只写「发光大标题」「科技风标题」；必须写清：标题在什么容器里、和左右时钟/按钮怎么贴、组织数字和页签在标题下第几行、是否与左右列顶对齐。
- **卡内死门（有参考图时）**：动手前每个面板必须写出：标题条形态（左三角 / 右斜纹 / 切角有没有）、主数字和标签谁上谁下、有没有图标、筛选行有没有附加控件（标签、轮播、搜索）。写不出 = 读不清 = **停下来问用户，禁止开写**。确认单禁止只写「四象限 / 播放器壳」这类可被空壳化的词。
- **发挥黑名单（有参考图时禁止）**：用两侧扫光线代替标题匾；把匾下的组织数字/页签拆成独立通栏、再把左右列顶下去；用通用四角标顶替图上的标题条；把图上的图标 / 标签 / 轮播收成「只有数字」；读不清顶栏或卡内装饰却用角标/扫光「凑一版像大屏」。不照抄 hex 只约束颜色，不授权换标题容器或丢掉卡内控件。
- **布局同构**：数字在左/对比在右、标题在顶/在底、轨线在控件上/下，以分区放大后的图为准。禁止为了「更好读」改成另一种排布；挤了只收字号或给对比列定宽。**字号层级跟参考图或现网**：类别标题、主数字、脚注须同级，禁止用 token 默认区间缩小一档；现网组件已写死字号则用现网。不照抄 hex 只约束颜色，不授权改字号档。
- **现网有壳用现网壳**：仓库里已有整页底图、标题条、筛选框装饰时优先引用（或内联），禁止只用 CSS 角标凑一版「像大屏」的空壳。
- **参考图入口禁止从 `templates/screen.html` 的 `.panel` / `.kpi` / `.topbar` 默认样式起稿**：只借适配、token、弹窗骨架。按读到的标题容器（纯字 / 匾 / 标题条）和筛选形态另写壳。套用模板玻璃卡 + 独立深色筛选通栏顶替图上的嵌套筛选或弱框 KPI，算换皮，P0。
- **低分辨率参考图**：整图像素宽 **< 1600** 时，确认单必须写「精度不足，观感可能差一档」，并建议用户补高清或区块放大图；不得假装已看清灯管色、筛选下划线、装饰密度后写通过。
- **读图冲突停写**：同一字段在两张 clip 或两次描述里不一致（如「一级公司 / 二级公司」、折线 / 柱线），列入疑点清单并**停问**，禁止挑一种继续生成。
- **确认单未发出不得开写**：即使用户先回 `go`，若确认单（顶栏形态 + 每卡形态 + 列宽比例反述）还没给用户看过，先补确认单再生成。
- **主数字色相是结构不是 hex**：图上利润绿灯管 / 收入青 / 成本黄，必须用 token 里对应语义色（如 `--data-5` / `--color-accent` / `--data-4`），禁止三卡同色。色相跟图，具体 hex 仍用 token。
- 复杂大屏建议用户提供「整体图 + 关键区块放大图」而非仅一张压缩整图——图片经分析转译为文字描述时，精确比例/字号层级/间距细节容易损耗，局部放大图能补齐。
- 复刻后须用比例文字反述给用户确认（如"识别到左列约占 24%、中列 52%、右列 24%，与原图是否一致？"），而非直接生成完事。
- **生成后同区块并排**：1920 原型截图按与参考图相同的区块裁切，列出**区块视觉差清单**（每块：`ref-*.png` / `proto-*.png` / 1～3 条可见差）并当场改完再交付。只核「块在、数对」或只交恒等式 / `js-errors none` **不算过**。清单为空却声称无差 = 未审核。
- 若参考图来自需求文档内的截图（如 PRD 截图），优先按入口④的规则从文档中提取原始图片，而非仅对截图做整体转译——原始图片精度远高于转译描述。

**入口④ 需求文档**（产品经理主路径）：解析文档提取「页面清单 → 每页区块 → 区块指标与图表类型 → 交互（弹窗/穿透/筛选）」，输出解析确认单。**文档未写明的项（如图表类型）必须列入"缺失项"并给出建议，禁止静默猜测后直接生成。**

- **图片优先于文字还原布局**：文档（docx/pptx 等）含嵌入截图时，必须提取原始图片（如解压 docx 读取 `word/media/*.png`）作为布局真相源——精确的区块排布、比例、视觉层级只有图片才有；文字仅用于提取指标口径、计算逻辑、交互说明。图片与文字描述冲突时，列入疑点清单问用户，不能只信一边静默选择。
- **疑点清单与缺失项并列，缺一不可**：疑点清单专门列文档内部的矛盾/重复/可疑错字（如同一指标在两处定义不同、明显笔误、图表类型前后不一致），而不仅是"文档没写"的缺失项。两类问题都必须在解析确认单中列出，禁止 AI 自行挑一种理解静默生成。

### Step 2：输出方案确认单

生成前必须先给用户看方案，格式：

```
【大屏方案确认单】
业务识别：医药经营分析类（默认指标包：营收/利润/存货/动销品种/批文）
标题：XX 分析大屏（1920×1080）
布局：三列式（布局详见 layout-patterns.md）
  ├─ 顶部：标题栏 + 时间筛选
  ├─ 左列：KPI 卡 ×4（今日营业额、订单数…）
  ├─ 中列：上-近30天营业额趋势（折线）；下-门店销量对比（柱状）
  └─ 右列：热销 Top10 排行 + 品类占比（饼图）
交互：点击 KPI → 明细弹窗（假数据）
缺失项：右下角区块文档未指明图表类型，建议饼图，是否可以？
疑点清单：「发货额环比」在文档 3.2 节与 3.5 节定义不同，采用哪个？
```

组件描述用人话（对照 layout-patterns.md 的组件清单人话列），不甩术语。业务识别行仅在需要推断业务或指标包时出现；疑点清单行仅在解析文档/图片发现内部矛盾时出现，无对应问题不必强行填充。多页/穿透项目、或用户对布局理解没把握时，确认单之后可先走 Step 3.5 骨架预览，再进入 Step 4 正式生成。

### Step 3：确认主题与输出形式

用 AskQuestion（或对话）确认，**每项都有推荐默认值，用户全选默认也能得到完整大屏**：

1. 主题：深色 BI 科技风（推荐）/ 浅色简约 —— token 见 [themes.md](themes.md)
2. 输出形式：单文件 HTML 原型（推荐，双击即看）/ Vue3 + Element Plus + ECharts + LESS + Pinia（Vite 构建）工程代码
3. 分辨率基准：1920×1080（推荐）/ 其他

用户在需求中已表明的不重复问。

### Step 3.5：骨架预览（复杂/多页项目建议先做，可跳过）

对于多页穿透项目、或用户对布局理解没有十足把握时，先产出一版**无配色、无真实图表、无 mock 数据**的纯灰框占位页：每个区块只画边框 + 区块用途标签文字（如「KPI 卡 ×4」「趋势折线图」），几秒钟出结果，让用户先确认区域划分和比例，而非等最终版才发现理解偏了。灰框骨架的模板见 [layout-patterns.md](layout-patterns.md) 的「骨架预览模式」一节。

- 灰框骨架的网格/比例必须与最终版完全一致，仅去掉主题配色、图表渲染、mock 内容
- 用户确认后再进入 Step 4，按同一套网格套用主题与内容，不重新排布
- 简单单页、或用户已给出精确布局描述时可跳过，直接进入 Step 4

### Step 4：生成

**HTML 模式**（默认）：
- 基于 [templates/screen.html](templates/screen.html) 骨架生成**零依赖单文件**（ECharts 用 CDN）
- **有参考图（复刻模式）**：不要沿用该模板的顶栏/面板/KPI 视觉；只复用 `#screen` 适配、`:root` token、弹窗/toast 结构
- 布局骨架从 [layout-patterns.md](layout-patterns.md) 选取
- 图表 option 一律从 [chart-patterns.md](chart-patterns.md) 模板修改，含标准动效
- mock 数据严格按 [mock-data-rules.md](mock-data-rules.md) 生成（量级真实、趋势有形状、行业名词）
- 交互（弹窗/跳转/筛选）用原生 JS + mock 数据实现假交互
- 多屏项目：逐屏生成独立 HTML，穿透跳转用相对链接串联（`<a href="secondary.html">`）

**Vue 模式**：
- 技术栈固定：**Vue3（Composition API）+ Element Plus + ECharts + LESS + Pinia，Vite 构建**；Element Plus 按需自动引入（unplugin-auto-import + unplugin-vue-components）
- 基于 [templates/screen.vue](templates/screen.vue) 骨架，组件化拆分：面板容器（PanelBox 模式）+ 图表组件（option 工厂纯函数）+ 屏幕适配 composable
- 控件类 UI（下拉/日期选择/弹窗/表格分页）用 Element Plus 组件；**深色主题必须用 token 覆盖其默认白底样式**（下拉面板、输入框、弹窗、date-picker 浮层），禁止白底穿帮
- 全局状态用 Pinia：时间上下文（statMonth 模式）、筛选条件跨组件联动、refreshToken 变更广播
- 色值放 `variables.less` + `colors.js` 双 token 文件（CSS 用 LESS 变量、ECharts 用 JS 常量）
- API 层留占位函数 + mock 开关，注释标明字段映射

**同时交付「设计说明清单」**（Markdown）：区块↔指标↔图表类型映射表、每个数字的 mock 口径、交互清单（哪里可点、点了干什么）。供产品评审和开发对接。

### Step 5：生成后强制自检

逐项核对，不通过就修，修完再交付：

- [ ] 所有色值来自所选主题 token 表（无裸写 `#fff`、`red` 等自造色）
- [ ] 基准分辨率下无横向滚动条、无组件溢出
- [ ] 长文本（企业名/产品名）有 `text-overflow: ellipsis` 处理
- [ ] 图表 legend/tooltip 不遮挡数据，grid 设置 `containLabel: true`
- [ ] 数字有千分位；同比/环比带涨跌色与 +/- 号
- [ ] 空数据区块显示占位符"—"，不留空白
- [ ] mock 数据符合量级规则（无"12345"式假数、涨跌有分布）
- [ ] 动效克制：仅数字滚动/图表入场/轮播/边框微光，无满屏粒子与闪烁
- [ ] 用无头浏览器在基准分辨率 + 至少 2 个其他分辨率实跑：无横向滚动、无溢出与文字裁切、弹窗随画布缩放且居中、各交互点击无 JS 报错——需贴出校验输出，不能只勾选
- [ ] 有参考图时：1920 原型与参考图按同一区块裁切并排，写出「区块视觉差清单」；卡内结构/标题位置/装饰相对位置/主数字色相无结构性偏差（具体 hex 跟 token 不算偏差，换皮算偏差）

Step 5 是生成过程中的自检，**不能代替 Step 5.5**。自检通过后必须进入独立审核，禁止把自检勾选复述成「已完成」。

### Step 5.5：独立审核（HTML / Vue 均必须，生成后立刻做）

对照确认单、参考图（若有）、硬性规则和 [review-checklist.md](review-checklist.md) **找不一致**，不是再打一遍 Step 5 的勾。

**产出（审核结论外置，禁止写入设计说明正文）**
- 目录：生成物**同级** `review/`（HTML 在 `foo.html` 旁建 `review/`；Vue 在工程根目录建 `review/`）
- 文件名：`{主文件名}-审核-YYYYMMDD.md`（例：`safety-control-platform.html` → `review/safety-control-platform-审核-20260821.md`；Vue 用入口页或工程名）
- 正文只写：对照范围、P0/P1/P2 问题、**区块视觉差清单**（有参考图时必填）、证据（截图路径或命令输出）、结论（通过 / 不通过）
- **无区块视觉差清单禁止写通过**（有参考图时）

**门禁**
- 审核发现的 **P0 / P1 / P2**（能力边界允许、审核里标明「未覆盖」的项除外）**一律当场修复**，不必停下来问用户接不接受、也不再另要一次 `go`
- P2 不是「不影响读数就可以略过」：发现了就改；改完写入审核文件作溯源，对话里简述改了什么即可
- 有任意 **P0** 修不掉：禁止说「已完成 / 可以交付」
- 改完后追加同一份审核文件的「复审」节，或新建 `…-审核-YYYYMMDD.md`；P0 仍在则不得声称完成
- 项目若另有「无 go 不改文件」规则：只约束「用户尚未授权生成本次产物」的阶段；**一旦进入已授权的生成/审核，P0/P1/P2 当场修**

清单分 HTML / Vue 两节，按当前输出形式执行对应节；「转 Vue」时两节都要过（先对 HTML 原型，再对工程）。

### Step 6：交付与迭代

- **仅当** Step 5.5 无未清 P0，且 P1 / P2 已当场改完并复审（或仅剩已标明的能力边界「未覆盖」）才进入本步
- 告知用户打开方式（HTML：浏览器直接打开；Vue：npm 装依赖后 dev）
- 用户口头提修改（"标题大一点""左下换饼图"）：按区块定位修改，改后重新走 Step 5 + 5.5
- 用户满意 HTML 原型后如要工程化，执行"转 Vue"流程（结构与视觉 1:1 复刻原型），转完必须再走 Step 5.5 的 Vue 节

## 硬性规则（任何模式必须遵守）

1. **色值只取自所选主题的 token 表**，禁止自造颜色、禁止 ECharts 默认配色
2. **深色主题禁止白底元素**（含 tooltip、弹窗、下拉），一律用主题面板色
3. 涨跌色与排名色**严格解耦**：涨跌只表方向（红涨绿跌可按主题配置），排名用金/橙/黄系，同一元素不得同时承担两种语义
4. 数值 `>0` 涨色、`<0` 跌色、`=0` 平色、`null` 显示"—"且不着色
5. 字体：标题可用装饰性字体（如优设标题黑），正文一律系统字体栈
6. 屏幕适配：整页 `transform: scale` **拉伸铺满**（默认，任何屏幕 100% 占满不留边，见 layout-patterns.md），禁止 rem 碎片化适配；仅用户明确要求"不能变形"时才改用等比留边
7. 图表必须 `resize` 响应窗口变化
8. 生成的 HTML 必须零本地依赖（ECharts 走 CDN），双击可打开
9. 解析需求文档/参考图时，**图片（原始截图/文档内嵌图）的布局精度高于文字转译描述**，两者冲突以图片为准并列入疑点清单确认；发现文档内部矛盾/重复/错字必须列入疑点清单，禁止静默选择一种理解直接生成
10. 多页/穿透类项目必须遵守 [mock-data-rules.md](mock-data-rules.md) 的「数据一致性引擎」：单一数据源、锚点值不可覆盖、汇总层数值必须等于子级之和
11. 有参考图时进入**复刻模式**：**布局同构是硬门禁**（卡内结构、标题位置、控件与装饰相对位置、列内疏密、字号层级、**筛选控件形态、主数字色相、装饰密度**）。「不照抄原图颜色」只约束 hex，不授权换皮、另排、换标题容器、丢掉图标/筛选附加控件、或缩小标题/主数字一档。现网组件已写死字号则跟现网，禁止用 `themes.md` 默认区间覆盖。禁止以可读性为由改排布。生成后必须同区块并排裁切并写出视觉差清单，结构性偏差与字号/色相差一档当场修。仓库已有底图/标题条则用现网壳。顶栏或卡内装饰读不清、裁错、两次读图冲突时禁止用扫光/角标/模板玻璃卡凑，必须停问。只核「块在、数对」不算过。

## 参考文件

- [themes.md](themes.md) —— 主题 token 表（色彩/字体/字号，深色 BI + 浅色简约）
- [layout-patterns.md](layout-patterns.md) —— 布局骨架、组件清单（含人话说明）、屏幕适配、多屏串联
- [chart-patterns.md](chart-patterns.md) —— ECharts 标准模板与动效规范
- [mock-data-rules.md](mock-data-rules.md) —— mock 数据真实感规则与行业名词库
- [templates/screen.html](templates/screen.html) —— 单文件 HTML 骨架
- [templates/screen.vue](templates/screen.vue) —— Vue3 工程骨架
- [review-checklist.md](review-checklist.md) —— Step 5.5 HTML / Vue 独立审核清单；P0/P1/P2 当场修，P0 修不掉挡交付
