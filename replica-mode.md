# 复刻模式（有参考图时必读）

承诺是**结构同构**，不是像素级材质。ECharts geo、灯管、电路密度标「未覆盖」，禁止勾成「通过且无差」。

无参考图不要用本文件，走主题模式（`templates/screen.html` + layout-patterns 默认栅格）。

产品 HTML **不要**做 `?overlay=1` 半透明底图，也不要在 review 里加扫像素脚本。写前认结构，写后同区块截图并排。

## 1. 鉴定源图（写 CSS 之前的第一步）

对用户给的**每一张**参考图跑鉴定，禁止信文件名或对话里的「1920」。聊天附件经常是 **1024×576 JPEG 存成 .png**。

脚本在**本 Skill 根目录**（与 `SKILL.md` 同级）的 `scripts/`，不在业务项目里。不要写死 `~/.cursor/skills` 或其它编辑器路径。找到根目录：含 `SKILL.md` 且含 `scripts/inspect-source.js`。

```bash
node <本 Skill 根目录>/scripts/inspect-source.js <图>
```

也可把脚本复制到生成物旁 `review/` 再跑。能跑则确认单必须抄输出。

跑不了 Node / 找不到脚本时：用环境里能用的等价手段读真实宽高与格式（如 `file`；macOS 可选 `sips -g pixelWidth`）。仍读不到则停，请用户把原生文件放进仓库。确认单须写明「未跑 inspect-source，已用手核 / 读不到」。

| 判定 | 动作 |
|---|---|
| 扩展名 .png 实为 JPEG，或宽 &lt; 1600 | **停**。请用户把原生文件放进仓库。禁止拉伸后当量寸 |
| 正好等于基准分辨率（默认 1920×1080） | 可按原尺寸分区 clip |
| 宽 ≥ 1600 但不是基准分辨率 | 确认单写「非 1:1，列宽只写大约」；请补原生基准图 |
| 宽 &lt; 1600 用户仍要继续 | 确认单写「低清，列宽不可当作已量准」 |

低清图不要「放大 4–12 倍」当看清了灯管和下划线。只有基准分辨率（或用户提供的区块原图）才分区 clip。

## 2. 写前：原图 clip + 壳/内容分开

**Clip 的是参考原图，不是已经写了一半的 HTML。**  
把鉴定通过的 PNG 按真实宽高放进空白页（视口 = 图尺寸，`<img>` 不要 `object-fit: fill`），再按区块裁切。环境有 Playwright（或同类无头浏览器）时用 `locator.screenshot` / `clip`；没有则用编辑器/系统能做的截图，并在确认单写「未跑 Playwright clip」。禁止用 macOS `sips --cropOffset` 当唯一裁切手段。裁完必须目视确认是目标区块，裁错则停。

分区至少：顶栏、筛选、每一块面板。用来认字段、标题容器、图表 series、卡内结构。列宽只从这些 clip **估大约占比**（如左约 22%、右约 27%），**禁止**写成「已量准 401.0px」。

**两套坐标不要混：**

| 图是什么 | 怎么用 |
|---|---|
| 含现网外框的整页截图 | 认整页结构；内容区相对**这张图**的内框来估，不要把「到屏幕左缘」直接写成 `.stage` padding |
| 只有内容、不含左右轨 | 只估内容栅格 |
| 现网壳 PNG（如 `bi-bg-x.png`，可能是 2460×1384） | `.shell` 上 `background-size: 100% 100%` 铺满 `#screen`，**只铺外框**。禁止用壳图估 KPI/列宽。`pointer-events: none`，`z-index` 在内容下 |

禁止：用壳图当内容参考；再 CSS 画一套侧轨/顶栏电路去「补」壳上已有的线；拿工业大屏的 inset 去套另一张截图的列。

确认单写清：壳用哪张（若有）、内容相对谁估。禁止套 `24% / 1fr / flex` 或 themes 默认字号档当「已量准」。

## 3. 确认单检查表（缺一项不准进入 Step 4）

即使用户先 `go`，下表没给用户看过也先补。禁止只写「发光大标题 / 四象限 / 科技风」。

```
【复刻确认单】
源图：路径 / 真实宽×高 / 格式（inspect-source 原文）
壳图：无 | 路径（只铺外框，不拿来估列宽）
标题容器：梯形匾 / 现网标题条 / 纯字；与时钟、页签、左右列如何贴
筛选：嵌在顶栏 / 独立通栏 / 仅下划线+HUD角  （禁止无依据做深色筛选条）
列宽反述：左/中/右 ≈ ?%（大约，不是 24/52/24，也不是已量准的 px）
每块面板（重复 N 次）：
  标题条形态 / 数字与标签上下 / 图标与附加控件 / 图表 series（柱+线|堆叠柱+线|双折线|地图|表…）
未覆盖：三维地图材质 / 灯管密度 / … （预先告知，勿事后装成无差）
疑点：两处 clip 不一致则停问
```

顶栏写不出形态、卡内写不出结构 → 停问，禁止开写。

## 4. 生成时禁止的默认脸

- 禁止从 `templates/screen.html` 的 `.panel / .kpi / .topbar` 起稿
- 禁止 `layout-patterns.md` 的 `24% 1fr 24%` 和 72–88px 顶栏默认值
- 禁止 `themes.md`「面板标准样式直接套用」玻璃卡 + `--bg-filter-bar` 独立筛选通栏去顶替图上的弱框/嵌套筛选
- 禁止 chart-patterns **模板 1 面积折线**当默认；图上是柱+线/堆叠则用模板 7/8，地图用模板 9
- 主数字色相跟图选 token（利润绿灯管 → `--trend-down` 或 `--data-5`，禁止三卡同青，也禁止「利润默认 --data-3 橙」）
- 字号跟分区 clip 里看到的档或现网已写死的值；禁止用 token 32–36px 区间缩小一档
- KPI 形态跟图：翻牌就翻牌，不要无依据 CountUp
- 图上组织名、账期、锚点数字整体照搬

发挥黑名单：扫光代替标题匾；把匾下组织数字/页签拆成独立通栏再把左右列顶下去；通用四角标顶替标题条；图标/标签/轮播收成只有数字。

布局同构：数字在左/对比在右以分区 clip 为准，禁止为「更好读」改排布。挤了只收字号或给对比列定宽。

## 5. 写后：同区块截图并排

不要靠整屏缩略图宣称还原好了。参考图与原型 **按同一块区域各截一张**，放进 `review/`：

- `ref-*.png`：从原生参考图（1:1 空白页里的那张 PNG）裁该块
- `proto-*.png`：从原型 HTML 对应节点裁（建议 `[data-review="…"]`）

审核表每一块一行：`ref` 路径、`proto` 路径、**1 条可核对的差**或「未覆盖：…」。禁止整表只写「该块已同构」。只核「块在、数对」或只交恒等式 / `js-errors none` 不算过。

不要求产品页带 overlay，不要求扫像素脚本。

结构/字号档/图表类型/换皮当场改。材质类标未覆盖后停。用户叫停打磨则禁止再改 CSS/图表皮肤。
