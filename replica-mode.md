# 复刻模式（有参考图时必读）

承诺是**结构同构**，不是像素级材质。ECharts geo、灯管、电路密度标「未覆盖」，禁止勾成「通过且无差」。

无参考图不要用本文件，走主题模式（`templates/screen.html` + layout-patterns 默认栅格）。

## 1. 鉴定源图（写 CSS 之前的第一步）

对用户给的**每一张**参考图执行 `node scripts/inspect-source.js <path>`（或 `file` + `sips -g pixelWidth -g pixelHeight`）。确认单必须抄输出，禁止信文件名或对话里的「1920」。

| 判定 | 动作 |
|---|---|
| 扩展名 .png 实为 JPEG，或宽 &lt; 1600 | **停**。请用户把原生文件放进仓库。禁止把聊天压缩图拉伸后量 gutter |
| 正好等于基准分辨率（默认 1920×1080） | 1:1 铺 overlay，量寸可用 |
| 宽 ≥ 1600 但不是基准分辨率 | 可按比例对照，确认单写「非 1:1，量寸仅供参考」，请补原生基准图 |
| 宽 &lt; 1600 用户仍要继续 | 确认单写「低清，量寸不可靠」；列宽不得写成确定 px 并声称已量准 |

聊天附件经常是 **1024×576 JPEG 存成 .png**。把它拉到 1920 量出的列沿是错的。

低清图不要「放大 4–12 倍」当看清了灯管和下划线——那是插值。只有基准分辨率（或用户提供的区块原图）才分区 clip。

**裁切：** 先把源图放进 page（`file://` 或 `<img>`），再用 Playwright `clip` / locator screenshot。禁止 `sips --cropOffset`。裁完必须目视确认是目标区块，裁错则停。

## 2. Overlay 叠哪一层

写 CSS 前把**内容参考图**做成 1920×1080、opacity≈0.3 的底图（`?overlay=1` 可开关）。壳图与内容图不是同一张。

| 图是什么 | 叠在 | 用途 |
|---|---|---|
| 含现网外框的整页截图 | `#screen` 1:1 | 量顶栏、列沿、块高、字高 |
| 只有内容区、不含左右轨 | `.stage`（壳 inset 之内） | 同上，相对 stage |
| 现网壳 PNG（如 `bi-bg-x.png`，可能是 2460×1384） | `.shell`，`background-size: 100% 100%` 铺满 `#screen` | **只铺外框**。禁止用壳图量 KPI/列宽。`pointer-events: none; z-index` 在内容下 |

禁止：整页截图叠到 `.stage`；壳图当内容底图；再 CSS 画一套侧轨/顶栏电路去「补」壳上已有的线。

量完可关 overlay。未铺底就写 grid / 字号 = P0。读图文字只认字段，不定比例。

列宽、块高、标题/翻牌/表头字高写成 px 再写 CSS。禁止套 `24% / 1fr / flex` 或 themes 默认字号档。

生成后用 `scripts/measure-overlay.js` 打出 `#screen` / `.stage` / `[data-review]` 盒，核对是否还对得上 overlay。

## 3. 确认单检查表（缺一项不准进入 Step 4）

即使用户先 `go`，下表没给用户看过也先补。禁止只写「发光大标题 / 四象限 / 科技风」。

```
【复刻确认单】
源图：路径 / 真实宽×高 / 格式（inspect-source 原文）
overlay：整页→#screen | 内容→.stage | 壳图路径（若有）
标题容器：梯形匾 / 现网标题条 / 纯字；与时钟、页签、左右列如何贴
筛选：嵌在顶栏 / 独立通栏 / 仅下划线+HUD角  （禁止无依据做深色筛选条）
列宽反述：左/中/右 ≈ ?%（来自 overlay，不是 24/52/24）
每块面板（重复 N 次）：
  标题条形态 / 数字与标签上下 / 图标与附加控件 / 图表 series（柱+线|堆叠柱+线|双折线|地图|表…）
未覆盖：三维地图材质 / 灯管密度 / … （预先告知，勿事后装成无差）
疑点：两处 clip 不一致则停问
```

顶栏写不出形态、卡内写不出结构 → 停问，禁止开写。

## 4. 生成时禁止的默认脸

- 禁止从 `templates/screen.html` 的 `.panel / .kpi / .topbar` 起稿
- 禁止 `layout-patterns.md` 的 `24% 1fr 24%` 和 72–88px 顶栏默认值
- 禁止 `themes.md`「面板标准样式直接套用」玻璃卡 + `--bg-filter-bar` 独立筛选通栏去顶替图上的弱框/嵌套筛选
- 禁止 chart-patterns **模板 1 面积折线**当默认；图上是柱+线/堆叠则用模板 7/8，地图用模板 9
- 主数字色相跟图选 token（利润绿灯管 → `--trend-down` 或 `--data-5`，禁止三卡同青，也禁止「利润默认 --data-3 橙」）
- 字号跟 overlay 或现网已写死的值；禁止用 token 32–36px 区间缩小一档
- KPI 形态跟图：翻牌就翻牌，不要无依据 CountUp
- 图上组织名、账期、锚点数字整体照搬

发挥黑名单：扫光代替标题匾；把匾下组织数字/页签拆成独立通栏再把左右列顶下去；通用四角标顶替标题条；图标/标签/轮播收成只有数字。

布局同构：数字在左/对比在右以分区 clip 为准，禁止为「更好读」改排布。挤了只收字号或给对比列定宽。

## 5. 生成后

同区块 clip 参考图与原型，写视觉差清单。每块必须有 **1 条可核对的差**，或写「未覆盖：…」。禁止只写「该块已同构」。只核「块在、数对」或只交恒等式 / `js-errors none` 不算过。

结构/字号档/图表类型/换皮当场改。材质类标未覆盖后停。用户叫停打磨则禁止再改 CSS/图表皮肤。
