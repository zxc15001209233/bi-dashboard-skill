# Mock 数据真实感规则

画面像不像"真大屏"，一半取决于假数据像不像真的。生成 mock 数据必须遵守以下规则。

## 量级规则

| 数据类型 | 量级要求 | 反例 |
|---|---|---|
| 企业营收/销售额 | 按业务规模：门店日营业额 千–万级；公司月营收 百万–亿级 | `12345`、`99999` |
| 数量/单量 | 与金额匹配（客单价合理）：奶茶店日单 200–800，药品月销量 万–百万 | 单量比营业额还大 |
| 百分比/占比 | 占比合计=100%；完成率 60–110%；同比环比 ±30% 内为主，个别 ±50% | 同比 +300% 满屏 |
| 人数/个数 | 整数；会员数、门店数符合常识 | 小数个数 |

尾数自然：`52,340`、`1,283.6 万`，禁止 `50,000`、`100,000` 这类整数扎堆（可少量出现）。

## 分布规则

- **涨跌有分布**：同屏同比指标必须有涨有跌（约 6:3:1 涨/跌/平），禁止清一色上涨
- **排行有头部**：Top N 第 1 名比第 2 名高 15–40%，尾部平缓衰减，禁止等差数列
- **占比有主次**：环形图最大项 25–45%，禁止均匀 N 等分
- **趋势有形状**：折线 = 整体趋势（升/降/平）+ 月度波动（±5–15%）+ 可选 1–2 个异常点（如 2 月春节回落）；禁止纯随机锯齿与完美直线
- **同期对比**：同期系列与本期形状相似但整体低（或高）5–20%，个别月交叉

## 中文名词库（按行业取用，禁止"产品A/门店1"）

**医药**：阿司匹林肠溶片、氨氯地平片、阿莫西林胶囊、布洛芬缓释胶囊、盐酸二甲双胍片、辛伐他汀片、奥美拉唑肠溶胶囊、左氧氟沙星片、复方感冒灵颗粒、维生素C咀嚼片

**零售/餐饮**：招牌珍珠奶茶、杨枝甘露、芝士葡萄、多肉草莓、椰云拿铁、烤奶三兄弟、满杯百香果、抹茶红豆冰；门店名用「XX路店」「XX广场店」「XX中心店」

**制造**：精密轴承 B-2205、伺服电机 SM-40、液压泵 HP-160、数控刀具 T-08、传动齿轮组 G-330

**能源/电力**：华东一号机组、风电场 F-12、光伏阵列 PV-3、储能站 ES-01

**物流**：华东分拨中心、华南转运仓、京津冀干线、成渝支线

**通用组织**：华东区、华南区、华北区、西南区、东北区、华中区、西北区

**人名**（值班/负责人场景）：张伟、王芳、李娜、刘强、陈静、杨磊、赵敏、周涛

企业名可用「XX + 行业词 + 有限公司」组合（如「云岭生物科技有限公司」），禁止使用真实知名企业全称。

## 时间规则

- 时间轴用**相对当前日期**的最近 N 期（近 30 天 / 近 12 月），不硬编码过去年份
- 「今日/本月」数据比历史均值略低（当期未完成），更真实

## 代码组织

mock 数据集中在一个 `MOCK` 对象/区块，业务字段命名清晰（`revenueByMonth`、`topProducts`），并附注释标明口径：

```js
const MOCK = {
  // 近12月营业额（万元）：整体上升趋势 + 2月春节回落
  revenueByMonth: [/* ... */],
  // Top10 热销：头部集中，第1名领先约30%
  topProducts: [/* ... */],
};
```

这样后续开发替换真实 API 时按块替换即可；「设计说明清单」中的 mock 口径与此注释一一对应。

## 多页面/多文档项目：数据一致性引擎（穿透类大屏必须遵守）

当存在「汇总页 → 分支页 → 末级页」结构（如集团 → 二级公司 → 单体企业的穿透大屏，或来自多份需求文档拼合的多页项目）：

1. **单一数据源**：所有页面共用一份数据文件（如 `mock-data.js`），禁止每页各生成一份互不关联的假数据
2. **锚点值不可覆盖**：需求文档/图片中明确给出的具体数值（如"XX 公司本期营收 1,234 万元"）标记为「锚点值」，任何生成/调整算法都不能覆盖锚点值
3. **差异重分配**：汇总层数值必须等于子级之和；总量与锚点值之间的差异，按权重分摊到非锚点行，禁止让汇总数字凭空对不上
4. **强制一致性校验**：生成后随机抽 3–5 个汇总项，手算/程序核对「子级之和 == 汇总值」，校验不过必须先修复再交付，不能只在自检清单里打勾了事
5. 涉及「量差 + 价差 = 整体收入变动」一类恒等式的行业（如医药量价分析、销量分析），必须在数据生成阶段保证恒等式精确成立，不能靠四舍五入蒙混
6. **自洽的参考数据必须整体作为锚点采用**：若参考图/文档中的数值自身已经自洽（分项合计 = 汇总项、"量差 + 价差 = 整体变动"一类恒等式成立），说明这是一套真实口径数据，必须整体照搬为锚点，禁止另编一套"量级差不多"的假数——自编数据极易破坏原有恒等式，产出"集团利润为负但多数子公司盈利"这类荒谬结果
7. **恒等式必须在交付前用脚本实跑核对**：把所有恒等式（分项合计、同比倒推、环比倒推）写成校验脚本跑一遍并贴出结果，禁止只在自检清单打勾
