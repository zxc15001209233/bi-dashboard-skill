# 布局骨架、组件清单与屏幕适配

## 组件清单（向用户描述方案时用"人话"列）

| 组件 | 人话说明 | 适用数据 |
|---|---|---|
| KPI 翻牌卡 | 一个大数字 + 涨跌小箭头（如「今日营业额 ¥52,340 ↑12%」） | 单个核心指标 + 同比/环比 |
| 趋势折线图 | 看一段时间的走势变化 | 按日/月的时序数据 |
| 对比柱状图 | 多个对象并排比大小（可加同期对比） | 门店/区域/品类间对比 |
| 占比环形图 | 一个圈看各部分占多少 | 构成占比（≤6 项） |
| 排行榜 | 前 N 名列表，名次 + 数值条 | Top N 排名 |
| 明细表格 | 多列数据行，可滚动/轮播 | 明细清单 |
| 热力格 | 颜色深浅表示数值大小的格子矩阵 | 多指标 × 多对象 |
| 仪表盘 | 半圆表盘显示完成率/达成率 | 0–100% 类指标 |
| 地图 | 按省/市着色或打点 | 地域分布（需 geoJSON，HTML 模式用 CDN 注册地图） |

## 布局骨架（基准 1920×1080）

所有骨架公共部分：**顶部标题栏**（高 72–88px：主标题居中或居左 + 右侧时间/筛选），其下为内容区。间距统一 12–16px，8px 栅格对齐。

### A. 经典三列式（默认推荐，信息量大）

```
┌──────────────────────────────────────────┐
│              标题栏 + 时间筛选              │ 72px
├─────────┬──────────────────────┬─────────┤
│ KPI 卡  │                      │ 排行榜  │
│ ×3~4    │   核心区（大图表/    │         │
│ 纵排    │   地图/趋势，占最    ├─────────┤
├─────────┤   大视觉权重）       │ 占比环  │
│ 次级图  │                      │ 或次级图│
│ 表      ├──────────────────────┤         │
│         │  明细表 / 次趋势     │         │
└─────────┴──────────────────────┴─────────┘
  左 24%          中 52%             右 24%
```

CSS：`display: grid; grid-template-columns: 24% 1fr 24%; gap: 14px;`，列内再纵向 grid/flex 分块。

### B. 上下分区式（指标少、强调总览）

```
┌──────────────────────────────────────────┐
│                 标题栏                    │
├──────┬──────┬──────┬──────┬──────┬──────┤
│ KPI1 │ KPI2 │ KPI3 │ KPI4 │ KPI5 │ KPI6 │ 130px KPI 卡带
├──────┴──────┴──┬───┴──────┴───┬──┴──────┤
│   趋势折线      │   对比柱状    │  占比环  │
├────────────────┴──────────────┴─────────┤
│           明细表 / 排行（横向通栏）        │
└──────────────────────────────────────────┘
```

### C. 左右两栏式（一主一辅）

```
┌──────────────────────────────────────────┐
│                 标题栏                    │
├────────────┬─────────────────────────────┤
│ 指标列表/  │        主图表（趋势/地图）    │
│ KPI 纵排   ├──────────────┬──────────────┤
│ + 排行     │   次级图表    │   次级图表    │
│            ├──────────────┴──────────────┤
│            │          明细表              │
└────────────┴─────────────────────────────┘
    左 28%                右 72%
```

**选骨架规则**：指标+图表总数 ≥8 → A；核心 KPI 为主、图表 3–4 个 → B；有一个绝对核心图（地图/大趋势）→ A 或 C。用户未指定时按此规则选并在方案确认单中说明。

## 屏幕适配（整页 transform: scale，两种模式统一用这套）

原理：画布固定设计尺寸，用 `transform: scale` 整体缩放——图表文字比例关系不乱，优于 rem 方案。

**默认：拉伸铺满**。横竖独立缩放，任何屏幕都 100% 占满、永不留边。常见屏幕（16:9 / 16:10）拉伸幅度肉眼基本无感；大屏产品"铺满"优先级高于"零变形"。

```js
const DESIGN_W = 1920, DESIGN_H = 1080;
function adapt() {
  const x = innerWidth / DESIGN_W, y = innerHeight / DESIGN_H;
  document.getElementById('screen').style.transform = `translate(-50%,-50%) scale(${x},${y})`;
}
addEventListener('resize', adapt); adapt();
```

**备选：等比留边**。仅当用户明确要求"绝对不能变形"（如展厅异形屏、LED 拼接屏物理比例固定）时使用：取 `Math.min(sx, sy)` 统一缩放并居中，留边区域背景必须与画布底色完全相同（`--bg-page`），避免出现突兀黑边。

配套 CSS：

```css
html, body { margin: 0; width: 100%; height: 100%; overflow: hidden; background: var(--bg-page); }
#screen {
  position: absolute; left: 50%; top: 50%;
  width: 1920px; height: 1080px;
  transform-origin: center center;
}
```

注意：缩放后 ECharts 仍按画布像素渲染，无需特殊处理；`resize` 时对所有 chart 实例调 `chart.resize()`（防浏览器缩放比变化）。

## 多屏项目串联（HTML 模式）

- 每屏独立文件：`index.html`（主屏）、`secondary-xxx.html`（穿透页）…
- 穿透跳转：`onclick="location.href='secondary-a.html'"`；穿透页左上角固定「← 返回」
- 各屏共享同一主题 token 与图表模板；把公共 CSS/JS 内联进每个文件（保持零依赖单文件可打开）
- 弹窗类交互不开新页：屏内 `<dialog>` 或固定定位遮罩层实现，样式用面板 token

## 交互模式（HTML 假交互标准实现）

| 交互 | 实现 |
|---|---|
| 点击 KPI/图表 → 明细弹窗 | 遮罩 + 面板样式弹层，内放 mock 明细表，右上角 ✕ 关闭 |
| 时间筛选 | 下拉 select（主题样式），切换后用另一组 mock 数据刷新图表 `setOption` |
| 点击列表项 → 穿透 | 跳转对应 HTML；无下级页时 toast 提示「演示版：穿透至 XX」 |
| 表格排序 | 点表头切换升降序（mock 数据前端排序） |

所有可点击元素必须有 `cursor: pointer` + hover 高亮（边框或底色变亮），让演示者一眼看出哪里能点。

## 骨架预览模式（SKILL.md Step 3.5 用）

用于生成前确认区域划分是否理解正确，不需要额外维护一套模板——**沿用最终版的 grid/flex 结构**，仅去掉主题 token 上色、图表 option、mock 数据，替换成灰框 + 用途标签：

```css
.skeleton-box {
  border: 2px dashed #6b7280; background: #1f2937; color: #9ca3af;
  display: flex; align-items: center; justify-content: center;
  font-size: 14px; min-height: 60px;
}
```

```html
<!-- 例：三列式骨架预览，网格与最终版一致，仅内容换成区块用途标签 -->
<div class="layout"> <!-- grid-template-columns: 24% 1fr 24%，与最终版相同 -->
  <div class="col">
    <div class="skeleton-box">KPI 卡 ×4</div>
    <div class="skeleton-box">次级图表</div>
  </div>
  <div class="skeleton-box" style="flex:1">核心趋势图</div>
  <div class="skeleton-box">排行 Top10</div>
</div>
```

用户确认骨架无误后，Step 4 直接在同一份 grid 上套用主题 token、图表 option 与 mock 数据，不重新排布区块；用户若指出区域不对（如"左右列宽度反了""少了一块"），仅调整 grid 比例/区块数量，不进入配色和图表细节讨论。
